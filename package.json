{
  "name": "texteditor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd ssr-backend && node server.js",
    "client": "cd ssr-frontend && npm start",
    "install-all": "concurrently \"npm install\" \"npm run backend\" \"npm run frontend\"",
    "backend": "cd ssr-backend && npm install",
    "frontend": "cd ssr-frontend && npm install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.0.1"
  }
}
